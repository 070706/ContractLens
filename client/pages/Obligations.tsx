import { CalendarDays, Check, Filter, ListChecks, Search, SlidersHorizontal } from "lucide-react";
import { useMemo, useState } from "react";
import { Link } from "react-router-dom";
import WorkspaceLayout from "@/components/WorkspaceLayout";
import { useContractLens } from "@/components/ContractLensContext";
import { PageHeader, SectionTitle, StatusPill } from "@/components/MvpUi";

export default function Obligations() {
  const { obligations, contracts, completeObligation } = useContractLens();
  const [view, setView] = useState("List");
  const [filter, setFilter] = useState("All");
  const [query, setQuery] = useState("");
  const visible = useMemo(() => obligations.filter((item) => (filter === "All" || item.status === filter || item.priority === filter) && `${item.title} ${item.party}`.toLowerCase().includes(query.toLowerCase())), [filter, obligations, query]);
  const counts = { all: obligations.length, today: 1, week: 3, upcoming: obligations.filter((item) => item.status === "Pending").length, overdue: obligations.filter((item) => item.status === "Overdue").length + 1, completed: obligations.filter((item) => item.status === "Completed").length };
  return <WorkspaceLayout title="Obligations"><PageHeader eyebrow="Global obligation tracker" title="Keep every commitment moving" description="One view across all contracts, parties, deadlines, and owners." action={<div className="flex items-center gap-2 rounded-xl bg-white/[0.08] px-3 py-2 text-[10px] font-bold text-[#cde7df]"><ListChecks className="h-4 w-4" /> {counts.all + 13} tracked obligations</div>} /><div className="mt-5 grid grid-cols-2 gap-3 md:grid-cols-3 xl:grid-cols-6">{[["All obligations", counts.all + 13], ["Due today", counts.today], ["Due this week", counts.week], ["Upcoming", counts.upcoming + 8], ["Overdue", counts.overdue], ["Completed", counts.completed + 9]].map(([label, value], index) => <button key={label} onClick={() => setFilter(index === 0 ? "All" : label === "Completed" ? "Completed" : label === "Overdue" ? "Overdue" : "Pending")} className={`rounded-2xl border p-4 text-left transition ${((filter === "All" && index === 0) || filter === label) ? "border-[#b9ded2] bg-[#effaf6]" : "border-[#e7edf1] bg-white hover:border-[#cbdde0]"}`}><div className="text-[22px] font-bold tracking-[-0.04em] text-[#304758]">{value}</div><div className="mt-1 text-[10px] font-semibold text-[#83939e]">{label}</div></button>)}</div><section className="mt-5 rounded-2xl border border-[#e7edf1] bg-white shadow-[0_5px_18px_rgba(34,52,68,0.025)]"><div className="flex flex-col gap-4 border-b border-[#edf1f4] p-5 md:p-6"><SectionTitle title="All obligations" description="Filter by contract, party, priority, or status." action={<div className="flex rounded-lg bg-[#f5f7f9] p-1">{["List", "Calendar", "Timeline"].map((item) => <button key={item} onClick={() => setView(item)} className={`rounded-md px-2.5 py-1.5 text-[10px] font-bold ${view === item ? "bg-white text-[#35495a] shadow-sm" : "text-[#91a0ac]"}`}>{item}</button>)}</div>} /><div className="flex flex-col gap-2 md:flex-row"><div className="flex h-9 flex-1 items-center gap-2 rounded-lg border border-[#e4eaf0] bg-[#f8fafc] px-3"><Search className="h-3.5 w-3.5 text-[#9ba9b5]" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search obligations..." className="w-full bg-transparent text-[11px] outline-none placeholder:text-[#a4b0ba]" /></div><button onClick={() => setFilter(filter === "All" ? "High" : filter === "High" ? "Medium" : "All")} className="flex h-9 items-center gap-1.5 rounded-lg border border-[#e4eaf0] px-3 text-[10px] font-bold text-[#6f8190]"><SlidersHorizontal className="h-3.5 w-3.5" /> {filter === "All" ? "Filter" : filter}</button></div></div>{view === "List" && <div className="overflow-x-auto"><table className="w-full min-w-[760px] text-left"><thead><tr className="border-b border-[#f0f3f5] text-[9px] font-bold uppercase tracking-[0.12em] text-[#a0acb5]"><th className="px-6 py-3">Party</th><th className="px-4 py-3">Obligation</th><th className="px-4 py-3">Contract</th><th className="px-4 py-3">Deadline</th><th className="px-4 py-3">Frequency</th><th className="px-4 py-3">Priority</th><th className="px-4 py-3">Status</th><th /></tr></thead><tbody>{visible.map((item) => <tr key={item.id} className="border-b border-[#f0f3f5] last:border-0"><td className="px-6 py-4 text-[10px] font-bold text-[#536978]">{item.party}</td><td className="px-4 py-4"><p className="text-[11px] font-semibold text-[#405768]">{item.title}</p><p className="mt-1 text-[9px] text-[#9aa8b2]">{item.source}</p></td><td className="px-4 py-4"><Link to={`/contracts/${item.contractId}?tab=obligations`} className="text-[10px] font-semibold text-[#4b8f80]">{contracts.find((contract) => contract.id === item.contractId)?.name ?? "Contract"}</Link></td><td className="px-4 py-4 text-[10px] text-[#81919e]">{item.deadline}</td><td className="px-4 py-4 text-[10px] text-[#81919e]">{item.frequency}</td><td className="px-4 py-4"><StatusPill tone={item.priority === "High" ? "rose" : item.priority === "Medium" ? "amber" : "blue"}>{item.priority}</StatusPill></td><td className="px-4 py-4"><StatusPill tone={item.status === "Completed" ? "green" : item.status === "Overdue" ? "rose" : "amber"}>{item.status}</StatusPill></td><td className="px-4 py-4">{item.status !== "Completed" && <button onClick={() => completeObligation(item.id)} className="rounded-lg p-1.5 text-[#4d987f] hover:bg-[#effaf6]" title="Mark complete"><Check className="h-4 w-4" /></button>}</td></tr>)}</tbody></table></div>}{view !== "List" && <div className="grid gap-3 p-5 md:grid-cols-2 md:p-6">{visible.map((item) => <div key={item.id} className="rounded-xl border border-[#edf1f4] p-4"><div className="flex items-center justify-between"><span className="text-[10px] font-bold uppercase tracking-[0.1em] text-[#8c9ba7]">{view === "Calendar" ? item.deadline : item.priority}</span><StatusPill tone={item.status === "Completed" ? "green" : "amber"}>{item.status}</StatusPill></div><h3 className="mt-3 text-[12px] font-bold text-[#405768]">{item.title}</h3><p className="mt-1 text-[10px] text-[#8c9ba7]">{item.party} · {item.source}</p></div>)}</div>}</section></WorkspaceLayout>;
}
