import { ArrowRight, BrainCircuit, FileText, LockKeyhole, MessageSquareText, Menu, ShieldCheck, Sparkles, X, Zap, type LucideIcon } from "lucide-react";
import { Link } from "react-router-dom";
import { useEffect, useState } from "react";
import { loadHomepage, type HomepageData } from "@/lib/homepage";

const iconMap: Record<string, LucideIcon> = { FileText, Zap, ShieldCheck, MessageSquareText };

function ContentLink({ href, children, className, onClick }: { href: string; children: React.ReactNode; className?: string; onClick?: () => void }) {
  if (href.startsWith("#")) return <a href={href} className={className} onClick={onClick}>{children}</a>;
  return <Link to={href} className={className} onClick={onClick}>{children}</Link>;
}

export default function Index() {
  const [menuOpen, setMenuOpen] = useState(false);
  const [data, setData] = useState<HomepageData | null>(null);
  const [error, setError] = useState<string | null>(null);

  useEffect(() => {
    let active = true;
    void loadHomepage().then((homepage) => {
      if (active) setData(homepage);
    }).catch((loadError: Error) => {
      if (active) setError(loadError.message);
    });
    return () => { active = false; };
  }, []);

  if (error) return <div className="flex min-h-screen items-center justify-center bg-[#f7f9fc] px-6 text-center text-sm text-[#526575]">{error}</div>;
  if (!data) return <div className="min-h-screen animate-pulse bg-[#f7f9fc]" />;

  const { settings, navItems, features, workflowSteps, preview, previewEvents, footerItems } = data;
  const navLinks = navItems.filter((item) => item.item_type === "nav");
  const secondaryAction = navItems.find((item) => item.item_type === "secondary_action");
  const primaryAction = navItems.find((item) => item.item_type === "primary_action");
  const mobileAction = navItems.find((item) => item.item_type === "mobile_action") ?? primaryAction;

  return <div className="min-h-screen bg-[#f7f9fc] text-[#162334]">
    <header className="relative mx-auto flex h-[76px] max-w-[1240px] items-center justify-between px-5 md:px-8">
      <ContentLink href="/" className="flex items-center gap-3"><div className="relative flex h-9 w-9 items-center justify-center overflow-hidden rounded-xl bg-[#c8f2e4] text-[#123d3a]"><div className="absolute -right-2 -top-2 h-6 w-6 rounded-full border-[5px] border-[#123d3a]/20" /><BrainCircuit className="relative h-[19px] w-[19px]" strokeWidth={2.2} /></div><div><div className="text-[15px] font-bold tracking-[-0.02em] text-[#183448]">{settings.brand_name}</div><div className="text-[9px] font-semibold uppercase tracking-[0.18em] text-[#8b9ba6]">{settings.brand_tagline}</div></div></ContentLink>
      <nav className="hidden items-center gap-8 text-[12px] font-semibold text-[#728392] md:flex">{navLinks.map((item) => <ContentLink key={item.id} href={item.href} className="transition hover:text-[#183448]">{item.label}</ContentLink>)}</nav>
      <div className="hidden items-center gap-3 md:flex">{secondaryAction && <ContentLink href={secondaryAction.href} className="px-3 py-2 text-[12px] font-bold text-[#526575] hover:text-[#183448]">{secondaryAction.label}</ContentLink>}{primaryAction && <ContentLink href={primaryAction.href} className="rounded-xl bg-[#183448] px-4 py-2.5 text-[12px] font-bold text-white shadow-[0_7px_16px_rgba(24,52,72,0.16)] transition hover:bg-[#244b61]">{primaryAction.label} <ArrowRight className="ml-1 inline h-3.5 w-3.5" /></ContentLink>}</div>
      <button onClick={() => setMenuOpen((open) => !open)} className="rounded-lg p-2 text-[#526575] md:hidden" aria-label={menuOpen ? "Close menu" : "Open menu"}>{menuOpen ? <X className="h-5 w-5" /> : <Menu className="h-5 w-5" />}</button>
      {menuOpen && <div className="absolute left-5 right-5 top-[68px] z-20 rounded-2xl border border-[#e4eaf0] bg-white p-3 shadow-xl md:hidden">{navLinks.map((item) => <ContentLink key={item.id} href={item.href} onClick={() => setMenuOpen(false)} className="block rounded-lg px-3 py-2 text-[12px] font-semibold text-[#526575]">{item.label}</ContentLink>)}{mobileAction && <ContentLink href={mobileAction.href} className="mt-2 block rounded-xl bg-[#183448] px-3 py-2.5 text-center text-[12px] font-bold text-white">{mobileAction.label}</ContentLink>}</div>}
    </header>

    <main>
      <section className="mx-auto max-w-[1240px] px-5 pb-20 pt-12 md:px-8 md:pb-28 md:pt-20"><div className="grid items-center gap-12 lg:grid-cols-[1.02fr_0.98fr] lg:gap-16"><div><div className="mb-5 inline-flex items-center gap-2 rounded-full border border-[#cfe8df] bg-[#effaf6] px-3 py-1.5 text-[10px] font-bold uppercase tracking-[0.14em] text-[#398171]"><Sparkles className="h-3.5 w-3.5" /> {settings.hero_eyebrow}</div><h1 className="max-w-[620px] text-[45px] font-bold leading-[1.04] tracking-[-0.06em] text-[#183448] md:text-[65px]">{settings.hero_title.split("\n").map((line, index) => <span key={`${line}-${index}`}>{line}{index < settings.hero_title.split("\n").length - 1 && <br />}</span>)}</h1><p className="mt-6 max-w-[520px] text-[16px] leading-relaxed text-[#657887]">{settings.hero_description}</p><div className="mt-8 flex flex-wrap items-center gap-3"><ContentLink href={settings.hero_primary_href} className="inline-flex h-12 items-center gap-2 rounded-xl bg-[#183448] px-5 text-[13px] font-bold text-white shadow-[0_10px_22px_rgba(24,52,72,0.16)] transition hover:bg-[#244b61]">{settings.hero_primary_label} <ArrowRight className="h-4 w-4" /></ContentLink><ContentLink href={settings.hero_secondary_href} className="inline-flex h-12 items-center gap-2 rounded-xl border border-[#dbe5e9] bg-white px-5 text-[13px] font-bold text-[#526575] transition hover:border-[#b9d9cf] hover:text-[#286d5d]">{settings.hero_secondary_label}</ContentLink></div><div className="mt-6 flex flex-wrap items-center gap-5 text-[10px] font-semibold text-[#91a0aa]"><span>✓ {settings.hero_proof_one}</span><span><LockKeyhole className="mr-1 inline h-3 w-3" />{settings.hero_proof_two}</span></div></div><div className="relative"><div className="absolute -right-4 -top-8 h-36 w-36 rounded-full bg-[#dff8ef] blur-2xl" /><div className="relative rounded-[22px] border border-[#e5ecef] bg-white p-4 shadow-[0_18px_50px_rgba(31,63,78,0.08)] md:p-5"><div className="flex items-center justify-between"><div><div className="text-[9px] font-bold uppercase tracking-[0.18em] text-[#9eabb3]">{settings.brand_name} AI</div><div className="mt-1 text-[13px] font-bold text-[#304758]">{preview.contract_name}</div></div><span className="rounded-full bg-[#e7f8f1] px-2.5 py-1 text-[9px] font-bold text-[#3c8b77]">{preview.contract_status}</span></div><div className="mt-5 grid grid-cols-3 gap-2"><div className="rounded-xl bg-[#fafbfc] p-3"><div className="text-[8px] font-bold uppercase text-[#9eabb3]">{preview.renewal_label}</div><div className="mt-1 text-[18px] font-bold text-[#3b5362]">{preview.renewal_value}</div><div className="mt-1 text-[8px] text-[#a2adb4]">{preview.renewal_detail}</div></div><div className="rounded-xl bg-[#effaf6] p-3"><div className="text-[8px] font-bold uppercase text-[#76a696]">{preview.obligations_label}</div><div className="mt-1 text-[18px] font-bold text-[#3b7368]">{preview.obligations_value}</div><div className="mt-1 text-[8px] text-[#76a696]">{preview.obligations_detail}</div></div><div className="rounded-xl bg-[#fff8ef] p-3"><div className="text-[8px] font-bold uppercase text-[#bd9063]">{preview.review_label}</div><div className="mt-1 text-[18px] font-bold text-[#926f4d]">{preview.review_value}</div><div className="mt-1 text-[8px] text-[#bd9063]">{preview.review_detail}</div></div></div><div className="mt-4 rounded-xl border border-[#edf1f4] p-3"><div className="flex items-center justify-between"><div className="text-[9px] font-bold text-[#526575]">{preview.timeline_title}</div><span className="text-[8px] font-bold text-[#7caf9f]">{preview.timeline_action_label}</span></div><div className="mt-2 space-y-1.5">{previewEvents.map((event) => <div key={event.id} className="flex items-center gap-2 text-[8px] text-[#718391]"><span className="w-12 font-bold text-[#a0adb5]">{event.event_date}</span><span className="h-1 w-1 rounded-full bg-[#8acdb9]" /><span className="flex-1">{event.event_title}</span><span className="rounded-full bg-[#f4f7f8] px-2 py-1 text-[7px]">{event.event_type}</span></div>)}</div></div><div className="mt-3 rounded-xl border border-[#d8eee5] bg-[#f1fbf7] p-3 text-[9px] font-bold text-[#438b7d]"><MessageSquareText className="mr-1.5 inline h-3.5 w-3.5" />{preview.ask_label}</div></div><div className="absolute -bottom-4 -left-4 rounded-xl border border-[#e6edf0] bg-white px-3 py-2 shadow-[0_10px_20px_rgba(31,63,78,0.08)]"><div className="text-[9px] font-bold text-[#4f7d70]">✣ {preview.change_label}</div><div className="mt-1 text-[8px] text-[#9ba8b0]">{preview.change_detail}</div></div></div></div></section>

      <section id="features" className="border-y border-[#e8eef0] bg-white"><div className="mx-auto max-w-[1240px] px-5 py-16 md:px-8 md:py-20"><div className="max-w-[520px]"><div className="text-[10px] font-bold uppercase tracking-[0.16em] text-[#63a993]">{settings.features_eyebrow}</div><h2 className="mt-3 text-[30px] font-bold tracking-[-0.05em] text-[#183448] md:text-[38px]">{settings.features_title}</h2><p className="mt-3 text-[14px] leading-relaxed text-[#718391]">{settings.features_description}</p></div><div className="mt-10 grid gap-4 md:grid-cols-2 lg:grid-cols-4">{features.map((feature) => { const Icon = iconMap[feature.icon_name] ?? FileText; return <div key={feature.id} className="rounded-2xl border border-[#e8eef1] bg-[#fbfcfd] p-5 transition hover:-translate-y-1 hover:bg-white hover:shadow-[0_12px_30px_rgba(30,62,77,0.07)]"><div className="flex h-10 w-10 items-center justify-center rounded-xl bg-[#e5f8f1] text-[#2d866f]"><Icon className="h-5 w-5" strokeWidth={1.8} /></div><h3 className="mt-5 text-[14px] font-bold text-[#304758]">{feature.title}</h3><p className="mt-2 text-[11px] leading-relaxed text-[#8797a2]">{feature.description}</p></div>; })}</div></div></section>

      <section id="how-it-works" className="mx-auto max-w-[1240px] px-5 py-16 md:px-8 md:py-24"><div className="grid gap-12 lg:grid-cols-[0.75fr_1.25fr] lg:gap-20"><div><div className="text-[10px] font-bold uppercase tracking-[0.16em] text-[#63a993]">{settings.workflow_eyebrow}</div><h2 className="mt-3 text-[30px] font-bold tracking-[-0.05em] text-[#183448] md:text-[38px]">{settings.workflow_title}</h2><p className="mt-4 text-[14px] leading-relaxed text-[#718391]">{settings.workflow_description}</p></div><div className="grid gap-6 md:grid-cols-3">{workflowSteps.map((step) => <div key={step.id} className="border-l-2 border-[#c8f2e4] pl-5"><div className="text-[11px] font-bold text-[#62a991]">{step.step_number}</div><h3 className="mt-6 text-[15px] font-bold text-[#304758]">{step.title}</h3><p className="mt-2 text-[11px] leading-relaxed text-[#8797a2]">{step.description}</p></div>)}</div></div></section>

      <section id="security" className="bg-[#183448] px-5 py-16 md:px-8 md:py-20"><div className="mx-auto flex max-w-[1040px] flex-col items-start justify-between gap-8 md:flex-row md:items-center"><div><div className="flex items-center gap-2 text-[10px] font-bold uppercase tracking-[0.16em] text-[#91e8cb]"><LockKeyhole className="h-4 w-4" /> {settings.security_eyebrow}</div><h2 className="mt-3 max-w-[560px] text-[28px] font-bold tracking-[-0.04em] text-white md:text-[36px]">{settings.security_title}</h2><p className="mt-3 max-w-[560px] text-[13px] leading-relaxed text-[#a9bac6]">{settings.security_description}</p></div><ContentLink href={settings.security_cta_href} className="inline-flex shrink-0 items-center gap-2 rounded-xl bg-[#c8f2e4] px-5 py-3 text-[12px] font-bold text-[#123d3a] hover:bg-[#e0faf1]">{settings.security_cta_label} <ArrowRight className="h-4 w-4" /></ContentLink></div></section>
    </main>

    <footer className="mx-auto flex max-w-[1240px] flex-col items-center justify-between gap-3 px-5 py-7 text-[10px] text-[#9aa7b1] sm:flex-row md:px-8"><div>{settings.footer_text}</div><div className="flex items-center gap-4">{footerItems.map((item) => item.href ? <ContentLink key={item.id} href={item.href} className="hover:text-[#59716f]">{item.label}</ContentLink> : <span key={item.id}>{item.label}</span>)}</div></footer>
  </div>;
}
